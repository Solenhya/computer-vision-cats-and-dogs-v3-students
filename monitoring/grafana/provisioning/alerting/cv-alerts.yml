# ═══════════════════════════════════════════════════════════════════════════════
# 🚨 GRAFANA ALERT RULES - Règles d'alerting MLOps
# ═══════════════════════════════════════════════════════════════════════════════
apiVersion: 1
groups:
  - orgId: 1
    name: cv-monitoring
    folder: Computer Vision
    interval: 1m
    rules:
      # ═══════════════════════════════════════════════════════════════════════
      # 🔴 ALERTE 1 : Base de données déconnectée (CRITICAL)
      # ═══════════════════════════════════════════════════════════════════════
      - uid: cv-db-disconnected
        title: Database Disconnected
        condition: C
        data:
          # ─────────────────────────────────────────────────────────────────
          # 📊 QUERY A : Récupération métrique Prometheus
          # ─────────────────────────────────────────────────────────────────
          - refId: A
            queryType: ''
            relativeTimeRange:
              from: 600  # -10 minutes
              to: 0      # maintenant
            datasourceUid: prometheus
            model:
              editorMode: code
              expr: cv_database_connected
              instant: true
              range: false
              refId: A
          # ─────────────────────────────────────────────────────────────────
          # 🧮 QUERY C : Threshold (condition de déclenchement)
          # ─────────────────────────────────────────────────────────────────
          - refId: C
            datasourceUid: __expr__
            model:
              conditions:
                - evaluator:
                    params:
                      - 1
                    type: lt
                  reducer:
                    type: last
                  type: query
              expression: A
              type: threshold
        
        noDataState: Alerting
        execErrState: Alerting
        for: 1m
        annotations:
          description: L'API ne peut plus accéder à PostgreSQL
          summary: Base de données PostgreSQL déconnectée
        labels:
          severity: critical
        isPaused: false