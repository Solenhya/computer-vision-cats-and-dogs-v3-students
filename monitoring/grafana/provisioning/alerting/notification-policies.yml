# ═══════════════════════════════════════════════════════════════════════════════
# 🔀 GRAFANA NOTIFICATION POLICIES - Routage des alertes
# ═══════════════════════════════════════════════════════════════════════════════

apiVersion: 1
policies:
  # ─────────────────────────────────────────────────────────────────────────
  # 📋 POLICY RACINE (default)
  # ─────────────────────────────────────────────────────────────────────────
  - orgId: 1
    receiver: Discord CV Alerts
    group_by: ['alertname', 'grafana_folder']
    group_wait: 10s
    group_interval: 5m
    repeat_interval: 12h
    routes:
      # ═══════════════════════════════════════════════════════════════════════
      # 🔴 ROUTE 1 : Alertes CRITICAL (haute priorité)
      # ═══════════════════════════════════════════════════════════════════════
      - receiver: Discord CV Alerts
        object_matchers:
          - ['severity', '=', 'critical']
        group_wait: 5s
        repeat_interval: 5m

      # ═══════════════════════════════════════════════════════════════════════
      # 🟡 ROUTE 2 : Alertes WARNING (priorité moyenne)
      # ═══════════════════════════════════════════════════════════════════════
      - receiver: Discord CV Alerts
        object_matchers:
          - ['severity', '=', 'warning']
        group_wait: 10s
        repeat_interval: 1h
      
      # ═══════════════════════════════════════════════════════════════════════
      # 🔵 ROUTE 3 : Alertes INFO (priorité faible)
      # ═══════════════════════════════════════════════════════════════════════
      - receiver: Discord CV Alerts
        object_matchers:
          - ['severity', '=', 'info']
        group_wait: 30s
        repeat_interval: 6h